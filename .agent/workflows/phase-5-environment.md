---
description: Phase 5 環境構築 - 開発環境を整え、タスクを分割する
---

# Phase 5: 環境構築ワークフロー

## 目的・概要

このフェーズでは、Phase 4 で作成した設計書に基づいて実装タスクを分割し、開発環境を構築する。

---

## 担当者

| 役割 | 担当 |
|------|------|
| 主導 | AG（建築家） |

---

## 前提条件

- [ ] Phase 4 が完了していること
- [ ] `docs/design/` 配下に設計書が存在すること
- [ ] `docs/tech-stack.md` が存在すること
- [ ] `PRODUCT.md` の Current Phase が Phase 5 であること

---

## Step 1: ドキュメント確認

以下のドキュメントを確認し、環境構築とタスク分割の準備を行う。

### 確認対象

| ドキュメント | 確認ポイント |
|-------------|-------------|
| `docs/requirements.md` | プロジェクト要件の全体像 |
| `docs/tech-stack.md` | 採用技術・フレームワーク・ツール |
| `docs/design/*` | 基本設計・DB設計・API設計・画面設計 |
| `docs/design/index.md` | **参照マップ**（画面→API→DBの依存関係） |

### 参照マップの活用

`docs/design/index.md` の参照マップを確認し、以下を把握する：

- 各画面が依存するAPI・DB
- 実装順序の参考（依存先を先に実装）

---

## Step 2: タスク分割案の作成

設計書から実装タスクを抽出し、**タスク分割案**を作成する。

> ⚠️ **この時点ではまだ Jira には登録しない**。Step 3 でユーザー承認後に登録する。

### Jira プロジェクトの確認

チケット作成前に、以下を確認する：

1. `PRODUCT.md` に記載された Jira プロジェクトキーを確認
2. プロジェクトキーが未設定の場合、ユーザーに確認して `PRODUCT.md` に追記

> ⚠️ すべてのチケットは指定されたプロジェクトに作成すること

### タスク階層構造

```
📋 親タスク（Epic / Story）
├── 📄 サブタスク 1
├── 📄 サブタスク 2
└── 📄 サブタスク 3
```

### タスク定義の必須要素

各タスクには以下を必ず含めること：

| 要素 | 説明 |
|------|------|
| **開始条件** | このタスクを始めるために必要な前提条件 |
| **受入条件** | 何をもって完了とするか（ゴール定義） |
| **目的・背景** | なぜやるのか（設計書・要件定義から引用） |
| **スコープ** | このタスクで行うこと・行わないことの境界 |

> 📋 テンプレートは `docs/_templates/` を参照

### 手順

1. **親タスクの作成**
   - 設計書の主要コンポーネント/機能ごとに親タスクを作成
   - 例: 「認証機能」「ダッシュボード画面」「API連携」

2. **サブタスクの作成**
   - 各親タスクを完了させるためのロードマップとして分割
   - 例: 「ログインフォームの実装」「トークン管理の実装」

3. **優先度・依存関係の設定**
   - 実装順序を考慮して優先度を設定
   - 依存関係があればリンクを設定

### タスク粒度の目安

- 1タスク = 1〜4時間程度で完了できる単位
- 大きすぎず、細かすぎず
- 誰が見ても何をやるべきかわかる記述

---

## Step 3: タスク分割案のユーザー承認・Jira登録

Implementation Plan で具体的なタスク分解案をユーザーに提示し、承認を得る。

### Implementation Plan に含める内容

- 親タスク一覧と概要
- 各親タスクのサブタスク一覧
- 実装順序・依存関係
- 見積もり時間（任意）

### 承認後

ユーザー承認が得られたら、Jira にチケットを登録する。

1. `PRODUCT.md` に記載された Jira プロジェクトキーを確認
2. 親タスク・サブタスクを登録
3. 優先度・依存関係を設定

---

## Step 4: 環境構築手順書の作成

技術選定（`docs/tech-stack.md`）の内容に合わせて、開発環境のセットアップ手順を `docs/setup.md` に文書化する。

### 含める内容

- 必要なツール・バージョン
- フレームワーク・パッケージのインストール手順
- 環境変数の設定
- 開発サーバーの起動方法
- ディレクトリ構造の説明

---

## Step 5: CCレビュー

成果物作成後、Claude Code でレビューを実施する。

### レビュー対象

| ファイル | レビュー観点 |
|---------|-------------|
| `docs/setup.md` | 手順の不備、技術的な誤り |
| `docs/tech-stack.md` | 技術選定の妥当性、整合性 |

### 実行コマンド

```bash
claude -p "以下のファイルをレビューしてください。
対象: docs/setup.md, docs/tech-stack.md

レビュー観点:
1. 誤字脱字がないか
2. 手順に不備がないか（漏れている情報がないか）
3. 技術的にあり得ないことを言っていないか
4. 技術選定と環境構築手順に矛盾がないか
5. 職人としての目線で見て、全体的に問題がないドキュメントになっているか

問題があれば具体的に指摘してください。"
```

### レビュー結果の扱い

> ⚠️ **CCの指摘を鵜呑みにしないこと**

1. CCからの指摘を受け取る
2. **AGが指摘の妥当性を検証**する
3. 妥当な指摘のみ修正を実施
4. 判断に迷う場合はユーザーに相談

---

## Step 6: ユーザー承認（環境構築実行前）

CCレビュー完了後、ユーザーに最終確認を行う。

### 確認内容

- `docs/setup.md` の内容
- CCレビュー結果と対応内容

---

## Step 7: 環境構築の実行

手順書（`docs/setup.md`）に沿って環境構築を実行する。

### 実行者

CC（Claude Code）

### 実行コマンド

```bash
claude -p "以下の手順に従って環境構築を実行してください。

## 前提情報
- プロジェクトルート: $(pwd)
- 作業ディレクトリ: ./src/（この中に環境を構築すること）
- 手順書: docs/setup.md
- 技術選定: docs/tech-stack.md

## 作業指示

1. まず docs/setup.md を読み、全体の手順を把握してください。
2. 手順書に記載されたコマンドを **上から順番に** 実行してください。
3. 各コマンドの実行結果を確認し、エラーがあれば以下の対応をしてください:
   - エラー内容を分析する
   - 解決策を調査する
   - 修正を適用する
   - 必要に応じて docs/setup.md を更新する
4. 全ての手順が完了したら、動作確認を行ってください:
   - 開発サーバーが起動できること
   - ブラウザでアクセスできること（該当する場合）
5. 作業が完了したら、以下の形式で報告してください:

## 報告形式

### 実行結果サマリー
- 成功した手順: X件
- 修正が必要だった手順: X件
- スキップした手順: X件（理由も記載）

### 発生した問題と対応
（問題があれば記載）

### 手順書の更新内容
（更新があれば記載）

### 動作確認結果
- 開発サーバー起動: OK/NG
- ブラウザアクセス: OK/NG/該当なし

### 備考
（その他気づいた点があれば記載）
"
```

### 実行後の確認

CC の報告を受け取ったら、以下を確認する:

1. 全ての手順が正常に完了しているか
2. 手順書に更新が必要か
3. 開発サーバーが正常に起動できるか

---

## 成果物

| ファイル | 説明 |
|---------|------|
| `docs/setup.md` | 環境構築手順書 |
| `src/` ディレクトリ構造 | 設計に基づいたソースコード配置 |
| Jira タスク | 親タスク + サブタスク |

---

## 完了条件

### タスク分割
- [ ] タスク分割案がユーザー承認済み
- [ ] Jira に親タスク・サブタスクが登録されている

### 環境構築
- [ ] `docs/setup.md` が作成されている
- [ ] CCレビューが完了している
- [ ] 環境構築手順書がユーザー承認済み
- [ ] 環境構築が実行され、開発サーバーが起動できる状態

---

## 次フェーズへの移行

1. `PRODUCT.md` の `Current Phase` を「Phase 6: 実装」に更新
2. `Status & Decisions` に環境構築完了を記録
3. **README.md の更新**（下記参照）
4. Phase 6-8 ワークフロー（`/phase-6-implementation`）を開始

---

## README.md の更新

Phase 5 完了時に `README.md` のセットアップセクションを更新する。

### 更新対象

```markdown
## セットアップ

> ⚠️ このセクションは Phase 5（環境構築）完了後に更新されます
```

↓ 以下のように置き換える：

### 更新内容

`docs/setup.md` から主要なセットアップ手順を抽出し、README.md に反映：

```markdown
## セットアップ

### 必要な環境

- Node.js {バージョン}
- npm / pnpm / yarn
- {その他必要なツール}

### インストール

\`\`\`bash
# リポジトリのクローン
git clone {repository-url}
cd {project-name}

# 依存パッケージのインストール
{パッケージマネージャー} install

# 環境変数の設定
cp .env.example .env.local
# .env.local を編集して必要な値を設定

# 開発サーバーの起動
{パッケージマネージャー} run dev
\`\`\`

### 環境変数

| 変数名 | 説明 | 必須 |
|--------|------|------|
| {変数名} | {説明} | Yes/No |

> 📖 詳細なセットアップ手順は [docs/setup.md](./docs/setup.md) を参照
```

### 例

```markdown
## セットアップ

### 必要な環境

- Node.js 20.x 以上
- pnpm 8.x 以上

### インストール

\`\`\`bash
# リポジトリのクローン
git clone https://github.com/user/cocktail-app.git
cd cocktail-app

# 依存パッケージのインストール
pnpm install

# 環境変数の設定
cp .env.example .env.local
# .env.local を編集して必要な値を設定

# 開発サーバーの起動
pnpm dev
\`\`\`

### 環境変数

| 変数名 | 説明 | 必須 |
|--------|------|------|
| NEXT_PUBLIC_SUPABASE_URL | Supabase プロジェクト URL | Yes |
| NEXT_PUBLIC_SUPABASE_ANON_KEY | Supabase 公開キー | Yes |
| SUPABASE_SERVICE_ROLE_KEY | Supabase サービスキー | Yes |

> 📖 詳細なセットアップ手順は [docs/setup.md](./docs/setup.md) を参照
```
